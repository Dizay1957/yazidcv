services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: curriva
      POSTGRES_USER: ${DATABASE_USERNAME:-curriva}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-local-development-only}
    volumes: [postgres_data:/var/lib/postgresql/data]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 15
    networks: [curriva]
  backend:
    build: ./backend
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DATABASE_URL: jdbc:postgresql://postgres:5432/curriva
      DATABASE_USERNAME: ${DATABASE_USERNAME:-curriva}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD:-local-development-only}
      JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in .env}
      BOOTSTRAP_ADMIN_EMAIL: ${BOOTSTRAP_ADMIN_EMAIL:-admin@curriva.local}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD:?Set BOOTSTRAP_ADMIN_PASSWORD in .env}
      CORS_ORIGIN: http://localhost:3000
      UPLOAD_DIRECTORY: /app/data/uploads
      PORT: 8080
    volumes: [cv_uploads:/app/data/uploads]
    ports: ["8080:8080"]
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/actuator/health/readiness"]
      interval: 10s
      timeout: 3s
      retries: 12
    networks: [curriva]
  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: http://localhost:8080/api/v1
    ports: ["3000:80"]
    depends_on:
      backend:
        condition: service_healthy
    networks: [curriva]
volumes:
  postgres_data:
  cv_uploads:
networks:
  curriva:
    driver: bridge
